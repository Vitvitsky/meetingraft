[package]
name = "meetingraft-session"
version.workspace = true
edition.workspace = true

[dependencies]
meetingraft-domain = { path = "../domain" }
uuid = { version = "1", features = ["v4"] }

[lib]
name = "session"
