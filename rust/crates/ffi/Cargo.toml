[package]
name = "meetingraft-ffi"
version.workspace = true
edition.workspace = true

[lib]
name = "meetingraft_ffi"
crate-type = ["cdylib", "lib"]

[dependencies]
meetingraft-domain = { path = "../domain" }
meetingraft-session = { path = "../session" }
meetingraft-storage = { path = "../storage" }
uniffi = "0.32"
